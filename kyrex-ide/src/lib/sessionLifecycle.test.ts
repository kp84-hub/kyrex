/**
 * NOTE: this file is intentionally inert.
 *
 * The focused lifecycle tests live at `kyrex-ide/tests/sessionLifecycle.test.ts`
 * (outside `src/`, so `tsc` — whose tsconfig includes only "src" and has no
 * @types/node — does not try to type-check node built-ins).
 *
 * Run them with:
 *   node --experimental-strip-types tests/sessionLifecycle.test.ts
 */
export {};
